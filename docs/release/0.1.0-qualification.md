# Hero Passport 0.1.0 — Release Qualification Record

Qualification date: 2026-09-10

Release verdict: RELEASED / QUALIFIED

This is the executable evidence ledger for Hero Passport 0.1.0. Qualification status is derived from GitHub Actions, the published immutable GitHub Release, exact artifact digests, and GitHub/Sigstore attestation evidence; editing this document alone cannot promote release or host status.

## Final release identity

```text
tag: v0.1.0
GitHub Release: Hero Passport 0.1.0
release id: 386030938
published: 2026-09-10T05:41:23Z
release state: stable, non-prerelease, immutable
source commit: 31409f10465478aba18fbd1f48f1e12a87fd61d0
post-merge main CI: CI #643 / run 34438271450 / success
release qualification: release-artifact-qualification #1 / run 34440441304 / success
publication workflow: publish-0.1.0 #1 / run 34442054952 / success
```

The release tag targets the frozen qualified source commit directly. The later publisher/ledger housekeeping commits are not part of the 0.1.0 product source.

## Exact release artifact

```text
archive: hero-passport-0.1.0.zip
format: hero-passport-release/1
payload files: 139
SHA-256: 4bde06fa4e1df957b8ea88944e2f3493fd892c9bd28278e44417f95077596b80
manifest: hero-passport-0.1.0.manifest.json
checksums: SHA256SUMS
sourceSha: 31409f10465478aba18fbd1f48f1e12a87fd61d0
version: 0.1.0
```

The published GitHub Release asset reports the same ZIP digest. The publication workflow downloaded the previously qualified Actions artifact, validated the manifest and SHA-256, verified its existing attestation, created a draft release, downloaded the draft ZIP back from GitHub, verified the ZIP digest and attestation again, and only then published the release. No product binary was rebuilt during publication.

## Frozen-main qualification

Post-merge CI #643 proved the integrated `main` state at the release source commit. Release qualification run `34440441304` then built exact version `0.1.0` from that same commit and passed the complete release pipeline.

Core release qualification included:

```text
Build GREEN, 0 warnings, 0 errors
Architecture 2/2 GREEN
Domain 135/135 GREEN
Application 53/53 GREEN
Infrastructure 45/45 GREEN
Contract 10/10 GREEN
App 25/25 GREEN
AgentEvals 13/13 GREEN
```

Version authority was checked from the published payload: CLI `--version` returned exactly `0.1.0`, matching the runtime/MCP product version authority.

## Same-exact-archive platform evidence

The single framework-dependent portable ZIP was created once, staged as one candidate, then that same candidate was downloaded and verified independently by the platform matrix before provenance was generated.

```text
Ubuntu 24.04 / ubuntu-24.04
  exact archive verification GREEN
  PackagedE2E GREEN
  Codex reference-host qualification GREEN

Windows Server 2025 / windows-2025
  exact archive verification GREEN
  PackagedE2E GREEN

macOS 15 / macos-15
  exact archive verification GREEN
  PackagedE2E GREEN
```

The release remains a framework-dependent portable .NET 10 distribution with `UseAppHost=false`; a compatible .NET 10 runtime is required.

## Codex reference-host evidence

Codex is the qualified 0.1 reference host for the accepted integration boundary. Qualification used the real stable Codex CLI `0.153.4`, isolated `CODEX_HOME`, repository Skill discovery, native STDIO MCP registration, and the exact release archive on Linux.

Evidence includes:

```text
repo Skill discovery: .agents/skills/hero-passport/SKILL.md
model-visible MCP namespace: mcp__hero_passport
model-visible tools: 11/11 exact
tool round-trip: hero.get_context
full lifecycle: bootstrap -> start -> finish
host restart boundary: separate codex exec processes
post-restart reads: get_context + get_card
idempotent replay: same Start + Finish request identities
persistent Hero Passport data root: proven
host_processes=4 replayed=true
```

Mutating qualification pre-approved only the isolated Hero Passport MCP test server; it did not use a global dangerous sandbox/approval bypass.

## Provenance and integrity

GitHub Artifact Attestations/Sigstore build provenance was created only after the same exact archive passed all three platform jobs.

```text
attested subject:
hero-passport-0.1.0.zip@sha256:4bde06fa4e1df957b8ea88944e2f3493fd892c9bd28278e44417f95077596b80

GitHub attestation id: 46468110
Sigstore Rekor log index: 2778276913
workflow: .github/workflows/release.yml
workflow event: workflow_dispatch
source commit: 31409f10465478aba18fbd1f48f1e12a87fd61d0
```

The release qualification workflow successfully ran `gh attestation verify` after creating provenance. The publication workflow independently ran the same attestation verification before draft creation and again against the ZIP re-downloaded from the draft release.

Consumers can verify the published artifact with:

```text
sha256sum -c SHA256SUMS
gh attestation verify hero-passport-0.1.0.zip -R runtime-human/hero-passport
```

## Packaged Core evidence

The packaged vertical launches `HeroPassport.App.dll` as a separate local STDIO MCP process against a fresh `HERO_PASSPORT_HOME` and temporary Git project. It covers:

```text
published payload + canonical packaged Agent Skill
bootstrap
get_context
explicit-Hero Start
Finish
server restart
state recovery
retry replay
HP135 changed-intent conflict
HP136 finalization conflict
```

Privacy qualification remains executable evidence through `StoragePrivacySurfaceTests` and `PrivacySurfaceContractTests`; persisted and wire surfaces exclude raw source/diff/log/prompt/secret/full-path/remote-url material.

## Host evidence matrix

```text
Codex | Qualified reference host | stable CLI + repo Skill + packaged MCP lifecycle/restart/replay proven
Claude Code | documented compatible | host smoke pending
VS Code | documented compatible | host smoke pending
Cursor | documented compatible | host smoke pending
Zed | documented compatible | host smoke pending
JetBrains | MCP/Core candidate | ambient Skill not qualified
ChatGPT | not release-qualified | local stdio 0.1 path not proven
```

`Qualified` means an executable release-time host path exists, not merely that current host documentation describes MCP or Skills. `Documented compatible` is not a release qualification claim. Additional host promotion is not required for the Codex-reference-host 0.1 release.

## Completed release gates

```text
pinned dependency restore/build
architecture dependency boundary
Domain deterministic goldens
Application lifecycle/retry/concurrency semantics
SQLite schema/connection/crash/migration qualification
migration-lock diagnosis and explicit repair
projection rebuild
logical Hero delete + anti-resurrection
HP-MCP/2 closed schemas and protocol qualification
preferred 2026-07-28 + compatibility 2025-11-25 protocol paths
Agent Skill format and lifecycle evals
RU/EN presentation resources
privacy/static wire + EF-model scans
curated publish bundle
packaged Core + Skill vertical restart/retry E2E
Codex stable CLI native MCP registration/listing
Codex repository Skill discovery
Codex exact 11-tool model-visible MCP inventory
Codex packaged hero.get_context round-trip
Codex bootstrap/start/finish/restart/context/card/replay lifecycle
Linux full qualification
Windows release-critical qualification
macOS release-critical qualification
single product-version authority
CLI/runtime product-version parity
deterministic release package builder + manifest/SHA contract
frozen-main exact 0.1.0 build
same-exact-archive Ubuntu/Windows/macOS qualification
GitHub Artifact Attestations/Sigstore provenance generation + verification
draft asset re-download + SHA/attestation verification
immutable stable GitHub Release v0.1.0 publication
```

## Post-release follow-up

No release blocker remains for 0.1.0. Future work is post-release scope and must not rewrite the immutable 0.1.0 evidence:

1. Add/enforce a GitHub ruleset or branch protection for `main` with required CI and force-push protection.
2. Promote other hosts only after host-specific executable smoke evidence exists.
3. Treat the deferred reversible human-CLI administration work from PR #25 as optional post-0.1 parity work, not as retroactive 0.1 release scope.
4. Remove any one-shot release-publisher automation after publication; future versions should use a generalized release publication workflow only after an equivalent frozen-artifact qualification gate.
